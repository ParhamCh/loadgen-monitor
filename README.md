# loadgen-monitor (TUI)

A minimal ANSI TUI module (MVU engine) with:
- fixed header/body/footer layout
- double-buffered rendering
- non-blocking input (press `q` to quit)
- safe terminal lifecycle (restore settings on exit)

## RUN
```bash
python3 app.py
```

